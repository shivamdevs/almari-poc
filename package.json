{
  "name": "almari-poc-lab",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "docker-compose up --build"
  },
  "dependencies": {
    "turbo": "^2.0.12"
  },
  "packageManager": "bun@1.3.14"
}